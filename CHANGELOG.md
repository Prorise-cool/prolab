# CHANGELOG

本文件是 ProLab 的唯一发布公告来源。Web 端版本弹窗和文档站 `/docs/progress/changelog` 都读取这里。

- 准备中的变更先写到 `Unreleased`。
- 正式发版时，把 `Unreleased` 内容移动到新的 `vX.Y.Z - YYYY-MM-DD` 区块。
- 每次发版必须同步更新根目录 `VERSION`。

## Unreleased

+ [新增] 画布节点支持统一维护名称字段，默认显示在节点上方，并可直接双击名称编辑。
+ [新增] 画布新增组节点，支持节点拖入/拖出分组、拖拽高亮吸附和移动组时带动子节点。
+ [调整] 画布节点顶部工具条改为点击选中节点后显示，避免鼠标经过节点时频繁弹出。
+ [新增] 生图设置新增独立的分辨率维度（1K/2K/4K），与宽高比、质量解耦：宽高比只定形状、分辨率只定大小、质量只控制画面精细程度（官方 quality 参数，不再影响尺寸）。
+ [调整] 生图宽高比选项移除 2K/4K 固定分辨率变体，改由分辨率维度统一控制；面板显示与实际请求使用同一套尺寸计算，手动填写宽高时自动按 16 倍数就近补齐并反推显示宽高比。
+ [修复] 修复走 DALL-E 兼容协议的生图模型（如 gpt-image）选择宽高比时只发 `aspect_ratio` 导致输出恒为 1024x1024 的问题，现按分辨率解析为真实像素尺寸随请求发送。
+ [修复] 修复生图手填宽高超出可用范围（过小、过大、超边或比例超 3:1）时面板显示与实际请求错位、生成才报错的问题，现提交时就近调整为最近有效尺寸并轻提示。
+ [调整] 生图分辨率区新增“自定义”档：手填宽高后自动高亮“自定义”并取消 1K/2K/4K 高亮，表明分辨率已被手填尺寸接管；自定义态下点分辨率档会按当前比例重算像素并退出自定义。gpt-image 等兼容模型手填像素时直接发送该尺寸、分辨率不参与，Gemini imageSize 改为按实际边长推断，不再被固定分辨率档覆盖。
+ [调整] 生图设置面板：质量档改为“自动/低/中/高”、分辨率档改为“自定义/1K/2K/4K”排序；生成张数只保留 1/2/3 张快捷档，输入框改为带上下微调按钮的 spinbox 便于微调。
+ [新增] 生图工作台和视频创作台在生成中状态改用 ThinkingOrb 点阵思考球动画替代默认转圈：按钮内动画同时把文字切换为“生成中...”，右侧生成结果占位卡片也用思考球替代原有转圈图标。
+ [新增] 生图工作台在生成过程中，左侧“生成记录”顶部实时显示“生成中”卡片（思考球 + 张数 + 用时），预览历史记录后可点击该卡片切回正在生成的任务，结果区改为按预览状态派生渲染、不再覆盖后台生成结果。
+ [调整] 生图工作台桌面端布局改为上下结构：上方左右分栏（左「生成记录」列、右「生成结果」为主体），下方「生图工作台」按内容自适应高度、横向分为提示词参考图 / 质量分辨率尺寸 / 宽高比 / 模型张数生成四个分组卡片，各组内容垂直居中，参数横向铺开后结果区不再被挤压；生成结果按图片数量整体居中排布、图片限高并保持比例，桌面端记录改用左侧常驻列（「生成记录」按钮仅移动端保留、从抽屉打开）。
+ [调整] OpenAI 兼容渠道的默认 Base URL 改为 `https://newapi.prorisehub.com`（含默认渠道、一键接入向导、新增渠道、URL 参数预填），仅影响新用户 / 未配置时的默认值，已保存的本地配置不变。
+ [修复] Nano Banana（Gemini Flash Image）指定宽高比/分辨率不生效、始终输出 1024×1024 的问题：实测上游只认 OpenAI 标准像素级 `size`（如 `1248x832`，精确遵循），不认比例字符串 / `aspect_ratio` / `quality`。现改为只透传像素级 `size`，宽高比、分辨率档（1K/2K/4K）、自定义尺寸均正确生效；并为 Nano Banana 单独放宽尺寸上限（支持真 4K，如 1:1→4096×4096、16:9→5456×3072，其它模型上限不变）；因质量参数对 Nano Banana 无效，工作台选中该系列时「质量」挡位置灰不可选并标注「当前模型不支持」。
+ [调整] 生图工作台提示词输入框高度提高（默认 5 行、可随内容增长至 10 行），参考图区域与工作台参数区整体高度相应调高，布局更舒展。

## v0.6.0 - 2026-07-09

+ [新增] 新增Codex App插件支持。
+ [新增] 配置与用户偏好新增独立页面和 Codex 连接配置 Tab。
+ [新增] 新增GitHub Pages 前端静态站点发布 workflow。
+ [新增] 图片切图支持等分线直接拖拽调整，并可新增、删除和重置横向 / 纵向切图线。
+ [调整] Docker 运行镜像改为 nginx 静态托管。
+ [调整] 移除网站Agent模式，专注于连接Codex Agent操作画布
+ [修复] 修复生图工作台重试成功结果刷新后丢失的问题。
+ [修复] 修复 Gemini 调用格式生图未传递尺寸比例配置的问题。
+ [修复] 修复前端 TypeScript 构建报错。
+ [修复] 修复画布生成配置切换文本/视频/音频模式时模型仍显示为生图模型的问题。
+ [修复] 兼容中转站视频任务直接返回视频 URL 且没有 `/content` 接口的情况，并优化失败原因展示。

## v0.5.0 - 2026-07-05

+ [新增] 渠道兼容Gemini格式。
+ [调整] 前端从 Next.js 迁移到 Vite，项目改为静态前端构建。
+ [调整] 移除已 404 的 EvoLinkAI 提示词来源。

## v0.0.2 - 2026-06-21

+ [优化] 同名模型选择体验。不同渠道或不同 API Key 下的同名模型会显示渠道序号、Base URL 和 Key 后四位，并在画布、Agent、生图工作台和视频工作台中精确路由到对应 API Key。
+ [修复] 旧数据里的裸模型名或已失效渠道模型会重新匹配当前可用渠道，避免同名模型静默调用到错误 Key。
+ [修复] 画布节点提示面板底部图像设置按钮与生成/停止按钮重叠的问题，模型名和设置文案过长时会自动截断。
+ [优化] 从已有节点拉出同类型新节点时，自动复用源节点已选择的模型，无需重新选择。

## v0.0.1 - 2026-06-20

### 版本定位

ProLab v0.0.1 是基于 `basketikun/infinite-canvas` 的初始迁移版本。这个版本保留原项目 AGPL-3.0 授权声明和 `canvas.best` 来源标识，并把 ProAPI 用户常用能力整合到 Pro Canvas 工作台。

### 面向用户的变化

+ [新增] 无需注册登录。应用默认在浏览器中运行，用户自行配置 ProAPI 或 OpenAI 兼容接口即可使用。
+ [新增] ProAPI 一键接入。支持默认上游、URL 参数预填、API Key 粘贴、模型拉取、模型选择和本地持久化。
+ [新增] 模型图标和能力识别。模型列表会自动识别 GPT、Claude、Gemini、Grok、Doubao、Hunyuan 等模型，并显示对应图标。
+ [新增] 标准模型参数适配。覆盖 OpenAI Images、image edits、OpenAI Video 和 Grok video chat 兼容协议，`hunyuan-image-v3` 等图像模型不会错误走 chat 协议。
+ [新增] 本地数据持久化。API 配置、画布、素材、生图记录和视频记录默认保存在当前浏览器本地。

### 数据保存说明

+ 浏览器本地数据会保留到用户清理站点数据、切换浏览器、切换域名、使用无痕窗口或浏览器触发存储清理为止。
+ 这不是云端账号存储，也不能保证跨设备永久保留。
+ 长期保存建议使用画布/素材导出，或在设置中配置 WebDAV 同步。WebDAV 同步用于画布、素材和生成记录，不同步 API Key。

### 当前范围

+ 本版本只接入海外标准模型参数协议，不接入 `mj-proxy`、`koukoutu` 等非本期目标协议。
