---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

- 画布节点名称：每个节点保留 `title` 名称字段，默认名称显示在节点上方，双击名称可直接编辑，节点信息弹窗和 JSON 可查看该字段。
- 画布节点工具条：节点顶部工具条改为点击选中节点后显示，单纯鼠标移入 / 移出节点不应再频繁弹出。
- 画布组节点：工具栏新增“组”节点，普通节点拖入组区域会高亮目标组，松手后自动吸附进组；拖出组区域会解除归属，拖动组节点会一起移动组内子节点。
- GitHub Pages：新增版本 tag 触发的前端静态站点自动发布 workflow，需在仓库 Pages 设置中选择 GitHub Actions 并验证路由刷新回退。
- 画布图片切图：行数 / 列数会生成可直接拖拽的等分切图线，并支持新增横向 / 纵向线、删除单条线、重置切图线和切片数量预览。
- Codex App 插件文档：新增插件安装教程，区分 AI 自动安装、手动安装和本仓库开发调试安装；同步优化插件 README 与技能说明。
- 本地 Codex 连接画布文档：新增 Canvas Agent、MCP、SSE 与浏览器画布交互原理说明，需确认文档导航和描述是否清晰。
- 顶部导航：缩小桌面顶部导航栏高度，导航项下划线和内容垂直对齐需确认。
- 配置与用户偏好：新增独立顶部导航页面，并在页面和弹窗中复用同一套配置面板；新增 Codex Tab，展示插件安装和本地 Agent 启动步骤，可配置本地 Agent 地址、Connect token 和工具执行确认开关。
- Codex 全局连接：配置页可直接连接本地 Canvas Agent，连接状态同步到画布和顶部 OpenAI 状态图标，刷新后会按本地保存的地址和 token 自动尝试重连；需验证已连接、连接中、连接失败、断开和刷新自动重连状态。
- 画布 Agent：Codex 旧 thread 在重启后不可用时会自动新建会话并重试，需验证进入画布直接发送消息不会再出现 `thread not loaded` 或 `no rollout found`。
- 画布 Agent：切换不同画布时会清空并重新加载当前 canvasId 对应的 Codex 会话，需验证不会继续显示上一个画布的对话。
- 画布 Agent：右侧 Agent 面板移除网站 / 本机模式切换，固定使用本机 Agent。
- 配置与用户偏好：每个模型渠道新增 OpenAI / Gemini 调用格式选择，默认 OpenAI，切换默认格式时同步更新默认 Base URL；渠道 Tab 增加紧凑醒目的提醒，说明模型是否可选需要到“模型”Tab 选择。
- Gemini 调用格式：支持拉取 Gemini 模型列表，并用于文本问答、在线 Agent 工具调用、基础生图和图生图请求。
- Gemini 生图：生图工作台和画布生图会把尺寸配置转换为 Gemini `responseFormat.image.aspectRatio` 传入，需验证 1:1、9:16、16:9 及自定义像素尺寸的输出比例。
- 音频和视频生成：选择 Gemini 调用格式时先给出不支持提示，仍需使用 OpenAI 兼容渠道。
- ProAPI 一键接入：首次未配置时自动弹出接入弹窗，支持 URL 参数预填 `apiKey` / `baseUrl`，拉取 OpenAI 兼容 `/v1/models` 后按模型推断结果分组并显示模型图标，完成后写入浏览器本地持久化配置。
- ProAPI provider 适配：`grok-imagine` / `hunyuan-image-v3` 等文生图走 DALL-E 兼容 `/v1/images/generations`，`grok-imagine-image-edit` / `qwen-image-edit` / `flux-kontext-*` 等图生图走 multipart `/v1/images/edits`，`grok-imagine-video` 走 multipart `/v1/videos`，`grok-video-chat` 走 `/v1/chat/completions`；本地视频参考图无法浏览器直连时会给出明确错误。
- WebDAV 同步：移除 Next.js 转发代理和连接方式选择，改为浏览器前端直接连接 WebDAV 服务。
- 提示词库：移除 Next.js route 抓取和服务端内存缓存，改为浏览器前端直连 GitHub 原始文件并缓存到 IndexedDB。
- 提示词库：移除已 404 的 `EvoLinkAI/awesome-gpt-image-2-API-and-Prompts` 来源。
- 前端构建：移除 Next.js，改为 Vite + React Router，并将前端目录调整为 `pages` / `components` / `stores` / `lib` / `layouts` / `styles`，需验证首页、图片/视频工作台、素材、提示词库和画布路由跳转。
- 前端构建：修复 TypeScript 严格检查下的构建报错。
- Vercel 部署：新增 `web` 和 `docs` 两个子项目的 Vercel 配置，需分别验证静态前端和文档站部署。
- Docker 部署：运行镜像从 Node serve 改为 nginx 静态托管，需验证前端路由刷新回退。
- 环境变量：移除过期的后端 `.env.example` 示例文件。
- 生图工作台：修复失败图片点击重试后只保存在页面状态、刷新或跳转后丢失的问题，重试成功结果会写入本地图片存储和生成记录。
- 视频生成：任务查询接口直接返回 `url` / `result_url` / `video_url` 时会跳过 `/content` 下载，并优先保存到浏览器本地媒体存储。
- 视频生成：任务创建或查询失败时会解析接口返回的 `msg` / `message` / 嵌套 JSON 错误，并在生成结果失败卡片里展示真实原因。
- 画布生成配置：修复切换文本/视频/音频模式时模型选择仍显示为生图模型（gpt-image-2）的问题，切换模式后应自动匹配对应能力的默认模型。
- 生图三维解耦：图像设置拆成宽高比（只定形状）、分辨率（1K/2K/4K，只定大小）、质量（只控制精细度，官方 quality 参数不再影响尺寸）三个独立维度；宽高比选项移除 2K/4K 变体。需验证 gpt-image-2 选 16:9 时切 1K/2K/4K 输出为 1360x768 / 2720x1536 / 3840x2160，且固定分辨率下切质量高/中/低尺寸不变。
- 生图自定义尺寸：面板手填 W/H 时会就近投影到可生成的有效尺寸（16 倍数、最长边 ≤3840、宽高比 ≤3:1、总像素 655360~8294400），超出范围（如 800x800、5000x5000、5000x1024、3000x500）会自动调整为最近有效尺寸并轻提示，面板显示与实际请求保持一致、不再出现显示回退到 1024 的错位；两个宽高输入互不覆盖，`16 倍数对齐`开关保留且始终生效。grok-imagine 仍走 aspect_ratio + resolution，nano-banana 仍按 1K/2K/4K 过滤，需验证这些模型不受影响。
- 生图分辨率与自定义尺寸联动：分辨率区新增“自定义”档，手填 W/H 后自动高亮“自定义”、1K/2K/4K 取消高亮，直观表明分辨率已被手填尺寸接管；自定义态点 1K/2K/4K 会按当前形状约分比例重新换算像素并退出自定义。gpt-image 等 DALL-E 兼容模型手填像素时直接发送该尺寸、分辨率不参与；Gemini/nano-banana 的 imageSize 改为按实际边长推断（如 896×1200→1K），不再固定发分辨率档。
- 生图设置面板排序与生成张数：质量档按“自动/低/中/高”排列，分辨率档按“自定义/1K/2K/4K”排列；生成张数只保留 1/2/3 张三个快捷档，末尾输入框改为带上下微调按钮的 spinbox（antd InputNumber），微调或手填的数值会与快捷档高亮保持同步。
- 生图/视频生成按钮与结果占位动画：生图工作台和视频创作台的“开始生成”按钮在生成中状态改用 ThinkingOrb 点阵思考球动画（solving 状态、size 20、深色配色），且按钮文字由“开始生成”变为“生成中...”；同时右侧生成结果占位卡片把原来的 LoaderCircle 转圈换成 ThinkingOrb（size 64、theme auto 自适应深浅色）显示在“生成中”文字上方。空闲态仍是普通可点击按钮，需验证生成中动画正常播放、按钮文字切换、结果占位球随主题显示、生成结束后恢复。
- 生图左侧记录显示生成中任务：生图工作台在生成过程中，左侧“生成记录”顶部会实时显示一张“生成中”卡片（点阵思考球 + 张数 + 已用时长），点击其它历史记录预览后，该“生成中”卡片仍在，点击它即可切回正在生成的任务；生成结果区改为按预览状态派生渲染，预览历史记录不再覆盖后台正在生成的结果，生成完成后自动归入正式记录。需验证：生成中出现“生成中”卡片、切换历史记录与切回生成中任务、生成完成后卡片消失并出现正式记录、非生成态不显示该卡片。
- 生图工作台布局改版（上下结构）：桌面端改为上下两块——上方左右分栏（左「生成记录」常驻列、右「生成结果」占主要宽度），下方工作台按内容自适应高度、内部横向分为四个分组卡片：①提示词 + 参考图 ②质量 / 分辨率 / 尺寸（含 16 倍数对齐开关）③宽高比（8 宫格）④模型 / 生成张数 / 开始生成。参数横向铺开后工作台整体降高，结果区不再被压成一条缝；桌面端移除了「生图工作台」标题行以节省高度，「生成记录」按钮只在窄屏（移动端）标题行显示，桌面端直接用左侧常驻记录列，点击从底部抽屉打开。图像设置组件新增 `sections` 分区渲染能力（默认全渲染，画布弹窗与视频页不受影响）。四个分组卡片内容垂直居中、不再上偏；工作台内外边距收窄、提示词输入框行数下调，整体高度进一步降低；生成记录卡片自适应宽度、标题超长截断，不再左右滚动；生成结果改为网格排布：桌面端每行最多 3 张（`xl` 3 列、`sm` 2 列、窄屏 1 列），超过一行自动换行，卡片使用固定缩略图高度（图片 object-contain 保持原始比例不拉伸），当行数超出结果区可视高度时结果卡片区内部出现纵向滚动条（标题「生成结果」固定不滚动），常规 1~3 张单行显示无滚动；移动端为整页滚动、卡片单列按自然高度堆叠。生成张数最右侧的数字微调框（InputNumber）加 `!w-full !min-w-0` 收进网格单元，不再超出「模型 / 张数 / 生成」分组卡片右边界。需验证：桌面端工作台不再出现「生图工作台」标题与「生成记录」按钮、四个分组内容垂直居中、工作台高度合适、生成记录卡片不横向溢出、生成结果每行最多 3 张且超出换行、行数过多时结果区内部可纵向滚动且标题固定、图片比例正常不拉伸、生成张数微调框不溢出卡片、宽高比/质量/尺寸/张数选择与联动正常、开始生成按钮生成中显示思考球与「生成中...」、窄屏单列堆叠且「生成记录」按钮可用并能打开抽屉预览。
- 默认模型服务地址：OpenAI 兼容渠道的默认 Base URL 由 `https://kiligai.com` 改为 `https://newapi.prorisehub.com`，同时同步默认上游（一键接入向导、新增渠道、URL 参数预填）的默认地址；Gemini 调用格式默认地址不变。仅影响新用户 / 未配置时的默认值，已保存的本地配置不受影响。需验证：全新用户（清空本地配置）首次打开时默认渠道 Base URL 为 `https://newapi.prorisehub.com`、一键接入向导预填该地址、新增渠道默认填该地址、切换到 Gemini 格式时仍回落到 Gemini 官方地址。
- 修复 Nano Banana 尺寸/宽高比不生效：选择 Nano Banana（Gemini Flash Image）系列模型并指定宽高比（如 3:2）时，生成图仍固定 1024×1024 的问题。经真机实测上游（New-API OpenAI 兼容透传）确认：Nano Banana 只认 OpenAI 标准的**像素级 `size`（如 `1248x832`）并精确遵循**（实测 3:2/9:16、1K~4K、自定义、超大尺寸均精准命中），而**比例字符串 `3:2`、`aspect_ratio`、`image_size`、`quality` 全部无效**。修复后 Nano Banana 请求改为只透传像素级 `size`（由分辨率档 + 宽高比或自定义尺寸合成），不再发比例字符串或 quality。因 Nano Banana 上游可精确出图到 6000×4000 级、支持真 4K，为其单独放宽尺寸上限（最长边 5504、像素上限 2400 万、4K 基数 4096），因此 Nano Banana 的 4K 档现在能输出真 4K（如 1:1→4096×4096、16:9→5456×3072、3:2→5008×3344，均已真机验证），而其它模型（gpt-image 等）仍沿用原上限（4K 最长边 3840，如 gpt-image 1:1→2880×2880），不受影响。此外由于质量参数对 Nano Banana 完全无效，生图工作台在选中 Nano Banana 系列模型时**保留「质量」挡位但整体置灰不可点击**（标题显示「质量（当前模型不支持）」），切换回其它模型（gpt-image 等）质量挡位恢复可选。需验证：Nano Banana 选 3:2 / 16:9 / 9:16 等比例时生成图实际按对应像素比例输出、分辨率档 1K/2K/4K 生效（4K 为真 4K 尺寸）、自定义尺寸生效、不再固定 1024×1024；选中 Nano Banana 时「质量」挡位置灰不可选、标题带「（当前模型不支持）」，切回 gpt-image 等模型时「质量」挡位恢复可选且 4K 仍为 2880/3840 级；其它模型（gpt-image、Flux、Grok 等）尺寸/比例/质量行为不变。
- 生图工作台高度微调：提示词输入框行数由 3 行提高到 5 行（`autoSize` 最多 10 行，可随内容增长），参考图区域最小高度调高，工作台参数区整体高度相应增加，视觉更舒展。需验证：提示词框默认显示约 5 行高度、内容变多时可自增长（上限 10 行）、参考图区域高度合适、工作台整体不显拥挤。
